apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-csi-uni
provisioner: nfs.csi.k8s.io
parameters:
  server: {{.Values.parameter.server}}
  share: {{.Values.parameter.share}}
  subDir: ${pvc.metadata.name}
reclaimPolicy: Delete
volumeBindingMode: Immediate